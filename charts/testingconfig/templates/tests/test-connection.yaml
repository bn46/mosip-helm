apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testingconfig.fullname" . }}-test-connection"
  labels:
    {{- include "testingconfig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testingconfig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
